# Analysis of the properties of the embeddings of biological sequences

To download the dataset, run:
```bash
wget ''
unzip dataset.zip
```

To zip the dataset, run:
```bash
zip -r dataset.zip dataset
```

