
# TODO make a proper script


# ./clustalo-1.2.4-Ubuntu-x86_64 -i dataset/emoglobina/emoglobina.fasta -o dataset/emoglobina/emoglobina.aln


# protein by default, nucleotide with -nt

# ./FastTree dataset/emoglobina/emoglobina.aln > dataset/emoglobina/emoglobina.dnd

# TODO add the rooting of the tree if needed (depends on the implementation that reads the tree, the new one seems to need the rooting)
